# 项目编码规范

## 1. 核心设计哲学 (Core Philosophy)
* **KISS (Keep It Simple, Stupid)**：拒绝过度设计。优先采用最直观的数据结构和算法，保持代码的可读性与可维护性，逻辑需一眼看透。
* **性能驱动 (Performance Driven)**：每一行代码都应为 CPU 指令效率、缓存友好性及内存布局负责。
* **零冗余 (Zero Overhead)**：避免不必要的抽象层，保持内核的轻量级与纯粹性。
* **严苛编译**：不允许有任何编译警告。所有警告必须视为错误处理，确保构建逻辑的确定性。

---

## 2. 内存管理准则 (Memory Management)
为了彻底杜绝内存碎片并提升分配效率，项目**强制用 `aq1u/arena` 内存池**。

* **统一分配原则**：在单个逻辑周期内统一分配内存，并在周期结束时原子级释放。
* **行为禁令**：严禁在核心逻辑中滥用原生的 `malloc` / `free`。
* **Arena 准则**：开发者在编写 PR 前必须自检是否严格遵守了 Arena 内存使用规范。

---

## 3. 函数编写哲学：Small & Atomic
为了保持开发的灵活性与测试的便捷性，遵循以下准则：

### "一屏原则" (The One-Screen Rule)
* **物理长度**：理想长度应控制在 **50 行**以内。
* **核心逻辑**：开发者在不滚动屏幕的情况下，应能一眼看清从参数输入、核心处理到错误处理的完整路径。

### 逻辑"低熵" (Low Entropy)
* **缩进限制**：尽量避免超过 **3 层** 的嵌套。如果逻辑太深，请果断提取子函数。
* **单一职责**：每一个函数应只解决一个颗粒度明确的问题。若函数名被迫使用 `and` 或 `then`，请拆分。
* **宁短勿长**：不要担心函数拆分带来的性能损耗。对于极高性能要求的代码（如 Raft 日志读写），优先使用 `static inline`。

### Vibe Check 自检清单
写完函数后，请自问：
1.  我能用一句话说清它在干什么吗？
2.  它是否定义了超过 8 个局部变量？（若是，说明状态管理太复杂）
3.  如果出 Bug 了，我能在 10 秒内定位到关键行吗？

---

## 4. 开发与协作流程 (Workflow)

### 增量式开发 (Iterative Development)
* **小步快跑**：遵循 "开发-测试-验证" 的节奏。禁止长周期、大规模的堆砌代码。
* **独立性原则**：每一个独立的功能（Feature）、修复（Fix）或重构（Refactor）必须对应一个独立的 PR。

### 分支管理与 PR 规范
* **分支策略**：`master/main` 仅存放稳定版本。新特性必须在独立 `Feature Branch` 进行。
* **评审流程**：合并前需进行 **Code Review**。在 PR 未合入前，禁止擅自删除或 Force Push 已在评审中的远程分支。
* **自检先行**：提交评审前，开发者应走读 Diff，确保无调试残留（如 `printf`）。

---

## 5. 代码风格 (Linux Kernel Style 变体)

### 5.1 命名规范
| 类型 | 风格 | 示例 |
| :--- | :--- | :--- |
| **类型** | `snake_case_t` | `arena_t`, `arena_state_t` |
| **函数** | `snake_case` | `arena_create()`, `arena_alloc()` |
| **宏** | `UPPER_CASE` | `ARENA_ALIGNMENT` |
| **变量** | `snake_case` | `chunk_size`, `current` |

### 5.2 格式细节
* **指针风格**：`*` 紧贴类型名，后面加空格。
    ```c
    arena_t* a;  /* Good */
    arena_t *a;  /* Bad */
    ```
* **大括号**：所有控制语句（`if`/`while`/`for`）**必须**使用大括号且换行，即使只有一行代码。
* **缩进**：使用 **2 个空格**（禁止使用 Tab）。
* **行宽**：每行不超过 **80 字符**。
* **注释**：注释必须**单独占据一行**，不能与代码同行。

---

## 6. Git 提交规范

### 6.1 Commit Message
* 简洁明，一行足够，使用小写字母开头的祈使句。
    ```text
    add arena.h: single-header memory allocator library
    update test suite for new API style
    fix pointer style to Linux kernel style
    ```

### 6.2 提交粒度
* **原子提交**：每个提交只做一件事。
* **规模控制**：单次 Commit 理想规模控制在 **50 到 150 行**逻辑代码之间。
* **历史整洁**：严禁脏提交（混入无关改动），合入后及时清理分支。

---

## 7. 错误处理
* **统一定义**：错误码必须统一定义在专用文件中，严禁在业务逻辑中硬编码数字。
